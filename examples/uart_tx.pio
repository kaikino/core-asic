# 8N1 UART transmitter fragment. Host replaces DELAY count for baud rate.
# r0 = idle/start/stop bit value; r1 = TX output-enable mask.
movi r0, 0x00
movi r1, 0x01
write
delay 0x3f
# A complete application emits each data bit by replacing r0 then WRITE/DELAY.
movi r0, 0x01
write
delay 0x3f
halt
